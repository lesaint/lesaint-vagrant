# instance management
alias vup="vagrant up"
alias vstart="vagrant up"
alias vdown="vagrant halt"
alias vhalt="vagrant halt"
alias vstop="vagrant halt"
alias vssh="vagrant ssh"

# boxes
alias vlist="vagrant box list"

# plugin vagrant snapshot
alias vs="vagrant snapshot"
